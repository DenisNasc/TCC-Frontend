import {useEffect} from 'react';

const usePageLogin = (): void => {
    useEffect(() => {
        // CRIAR UM HOOK PARA LER O COOKIE DO JWT E ENVIAR PARA O SERVER PARA MATER O USUÁRIO LOGADO
    }, []);
};

export default usePageLogin;
